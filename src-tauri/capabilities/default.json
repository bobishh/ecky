{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main webview.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-execute",
    "dialog:default",
    "fs:default",
    "core:path:default"
  ]
}
